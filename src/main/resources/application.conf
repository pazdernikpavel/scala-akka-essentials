akka {
    logLevel = "DEBUG"
}

specialConfig {
    akka {
        logLevel = "INFO"
    }
}

# timed assertions test config

specialTimedAssertionsConfig {
  akka.test.single-expect-default = 0.3s
}